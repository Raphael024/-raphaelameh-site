import { CASE_STUDIES } from '@/lib/constants';
export const metadata = { title: 'Case Studies', description: 'Anonymous case studies across data engineering, analytics, ML, and AI.' };
export default function CaseStudiesPage() {
  return (<section className="pt-32 pb-24 px-6 bg-cream"><div className="max-w-[1120px] mx-auto"><h1 className="font-serif text-[clamp(28px,4vw,42px)] font-bold text-navy mb-14">Selected Work</h1><div className="grid grid-cols-1 md:grid-cols-2 gap-6">{CASE_STUDIES.map((p,i)=>(<div key={i} className="bg-white rounded-xl p-8 border border-border flex flex-col"><p className="text-[11px] font-medium text-gold tracking-wide mb-3">{p.tags}</p><h2 className="font-serif text-xl font-bold text-navy mb-3">{p.title}</h2><p className="text-[15px] leading-[1.75] text-muted mb-6 flex-1">{p.description}</p><div className="border-t border-border-light pt-4"><p className="text-[12px] font-semibold text-gold uppercase mb-1">Outcome</p><p className="text-[14px] text-navy font-medium">{p.outcome}</p></div></div>))}</div></div></section>);
}
